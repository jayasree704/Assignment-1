numbers = [3, -1, 4, 1, -5, 9, 2, -6, 5]
total = 0
count = 0
for num in numbers:
    if num >= 0:
        total += num
        count += 1
average = total / count
for i in range(len(numbers)):
    if numbers[i] < 0:
        numbers[i] = average
numbers.sort()
print(numbers)
